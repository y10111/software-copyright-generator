name: Build Linux Installer

on:
  workflow_dispatch:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: 'temurin'
          cache: 'maven'
      - name: Download JavaFX SDK (Linux x86_64)
        run: |
          mkdir -p javafx-linux-x86_64
          curl -L -o javafx-sdk.zip https://download2.gluonhq.com/openjfx/21.0.6/openjfx-21.0.6_linux-x86_64_bin-sdk.zip
          unzip -q javafx-sdk.zip -d javafx-linux-x86_64
          rm javafx-sdk.zip
      - name: Build with Maven
        run: mvn -B clean install --file pom.xml
      - name: Upload DEB
        uses: actions/upload-artifact@v4
        with:
          name: Linux-deb
          path: target/*.deb